% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/inputs_and_query.R
\name{query_database}
\alias{query_database}
\title{Query static inputs (plus indices) using simple R filters}
\usage{
query_database(
  con = NULL,
  raw_db_path = NULL,
  filters = list(),
  limit = NULL,
  table_name = "simulation_results"
)
}
\arguments{
\item{con}{Optional DuckDB connection object. If provided, raw_db_path is ignored.}

\item{raw_db_path}{Path to the .duckdb file. Required if con is not provided.}

\item{filters}{Named list of filters where names are column names and values are:
\itemize{
\item scalar (numeric/character/logical) -> equality
\item numeric vector of length 2 -> inclusive range \code{c(lower, upper)} (e.g., \code{eir = c(30, 50)})
\item character vector (length >= 1) -> IN (...)
Multiple filters are AND-ed together.
Examples:
list(parameter_index = 3)
list(global_index = c("fileA.rds","fileB.rds"))
list(prevalence = c(0.1, 0.2), timesteps = c(6\emph{365, 12}365))
}}

\item{limit}{Optional integer LIMIT for previewing results.}

\item{table_name}{Table to query. Default "simulation_results".}
}
\value{
A data.frame with DISTINCT rows of:
parameter_index, global_index, eir, Q0, phi_bednets, seasonal, routine,
dn0_use, dn0_future, itn_use, irs_use, itn_future, irs_future, lsm
}
\description{
Query static inputs (plus indices) using simple R filters
}
